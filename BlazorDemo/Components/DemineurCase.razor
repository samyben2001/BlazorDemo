@using BlazorDemo.Enums
@if (Case is not null)
{
    <div class=@("d-flex justify-content-center align-items-center " + SetClass()) @onclick="OnSelect">
        @if (Case.IsActive)
        {
            <span>@Case.Etat</span>
        }
    </div>
}